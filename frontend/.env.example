# Copy this file to .env.local and set the backend URL.
# Leave blank for local dev (the Vite proxy in vite.config.js forwards to localhost:8000).
# For production (Vercel), set this to your Render backend URL.

VITE_API_URL=
