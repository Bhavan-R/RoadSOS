# Copy this file to .env and fill in your values.

# Required: Anthropic API key for Claude triage
ANTHROPIC_API_KEY=sk-ant-...

# Optional: Google Places API key for fallback when OSM returns < 3 results
# Without this, OSM-only results are used (fine for most Indian cities)
GOOGLE_PLACES_API_KEY=AIza...
